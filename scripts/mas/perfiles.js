function registrarUsuario(nombre, correo, rol) {
    if (!nombre || !correo || !rol) {
      alert("Todos los campos son obligatorios.");
      return;
    }
    // Aquí se llamaría al backend o se enviaría el formulario
  }
  